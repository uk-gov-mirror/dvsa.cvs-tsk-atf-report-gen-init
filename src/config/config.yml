sqs:
  local:
    params:
      region: localhost
      endpoint: http://localhost:9324
      apiVersion: "2012-11-05"
    queueName: atf-gen-q
  remote:
    params:
      apiVersion: "2012-11-05"
    queueName: atf-report-gen-${BRANCH}-queue
